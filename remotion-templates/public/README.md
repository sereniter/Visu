This directory should contain public image assets used by Remotion compositions, for example:

- `anukramai-logo.png`
- `anukramai-logo-white.png`

Place governed, repository-tracked PNGs here before running renders. The VISU `RemotionAdapter` validates `logoPath` values against this directory and logs SHA256 hashes for determinism audits.

